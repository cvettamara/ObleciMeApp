<resources>
    <!-- Base theme -->
    <style name="Base.Theme.ObleciMeApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
    </style>
    <style name="Theme.ObleciMeApp" parent="Base.Theme.ObleciMeApp" />

    <!-- Button style -->
    <style name="Widget.ObleciMeApp.Button" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/colorButton</item>
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>
